//config/constants.dart
class AppConstants {
  // Animations
  static const Duration splashAnimationDuration = Duration(milliseconds: 1500);
  static const Duration pageTransitionDuration = Duration(milliseconds: 500);
  static const Duration buttonAnimationDuration = Duration(milliseconds: 200);
  
  // Video calling
  static const Duration connectionTimeout = Duration(seconds: 10);
  static const Duration reactionDisplayDuration = Duration(seconds: 2);
  
  // UI 
  static const double cornerRadius = 24.0;
  static const double buttonCornerRadius = 22.0;
}